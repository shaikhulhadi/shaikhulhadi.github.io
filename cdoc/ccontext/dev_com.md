
```bash
# Install Ruby dependencies
bundle install

# Serve with live reload (available at http://localhost:4000)
bundle exec jekyll serve --livereload

# Build static site
bundle exec jekyll build

# Production build (enables HTML/JS/CSS compression)
JEKYLL_ENV=production bundle exec jekyll build
```

No npm/node tooling — assets are pre-compiled and committed directly.